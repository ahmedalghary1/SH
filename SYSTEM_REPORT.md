# تقرير تفصيلي عن نظام SH ERP

## 1. مقدمة عامة

هذا المشروع هو نظام ERP مبني باستخدام Django لإدارة نشاط ملابس/تجارة ملابس. النظام لا يقتصر على شاشة مبيعات أو مخزون فقط، بل يغطي دورة العمل الكاملة تقريبًا: تعريف المنتجات، إدارة المخازن، البيع، الفواتير، التحصيلات، المرتجعات، المشتريات، الموردين، العملاء، CRM، عهدة المناديب، التقارير، الطباعة الحرارية، العمل دون اتصال عبر PWA، وسجل تدقيق للعمليات الحساسة.

اسم المشروع الظاهر في الوثائق هو `SH ERP`، والغرض الأساسي منه إدارة عمليات البيع والمخزون والمالية لشركة أو محل ملابس مع دعم واضح للغة العربية وتوقيت مصر.

## 2. التقنية المستخدمة

النظام مبني على Django 5.0.1. أهم الحزم الموجودة في `requirements.txt`:

- `Django==5.0.1`: إطار العمل الأساسي.
- `Pillow`: التعامل مع صور المنتجات والشعار.
- `reportlab`: توليد PDF.
- `arabic-reshaper` و`python-bidi`: تحسين عرض العربية داخل ملفات PDF.
- `whitenoise`: تقديم ملفات static في بيئات الإنتاج.
- `psycopg2-binary`: الاتصال بـ PostgreSQL.

القاعدة الافتراضية في الإعدادات الحالية هي PostgreSQL، مع وجود ملف SQLite محلي في المسار، لكنه ليس قاعدة الإنتاج الأصلية.

## 3. هيكل المشروع

المشروع مقسم إلى تطبيقات Django مستقلة حسب المجال الوظيفي:

- `accounts`: المستخدمون والصلاحيات وتسجيل الدخول.
- `products`: المنتجات والتصنيفات والألوان والمقاسات والمتغيرات.
- `inventory`: المخازن والأرصدة وحركات المخزون والدفعات.
- `customers`: العملاء وCRM والتفاعلات والمتابعات.
- `orders`: الطلبات وفواتير البيع وعروض الأسعار والعينات.
- `invoices`: الفواتير والطباعة والتصدير.
- `finance`: الخزن والحسابات المالية والحركات وكشوف الحساب.
- `purchases`: الموردون وأوامر الشراء واستلام البضاعة والدفع.
- `returns`: مرتجعات البيع والاستبدال.
- `sales_reps`: عهدة المناديب والتحصيلات والتسليمات.
- `reports`: التقارير الإدارية والمالية والمخزنية.
- `dashboard`: لوحات التحكم حسب الدور.
- `settings_app`: إعدادات الشركة والفواتير والطباعة.
- `sync_api`: واجهة مزامنة PWA/desktop/offline.
- `audit`: سجل التدقيق وتتبع العمليات الحساسة.

القوالب موجودة في `templates/`، والملفات الثابتة في `static/`، والإعداد الرئيسي في `config/settings.py`.

## 4. الإعدادات والتشغيل

ملف الإعداد الرئيسي هو `config/settings.py`. يقوم بتحميل `.env` إن وجد، لكن في المسار الحالي لا يوجد ملف `.env`. لذلك عند التشغيل بدون متغيرات بيئة سيستخدم النظام القيم الافتراضية الموجودة داخل `settings.py`.

أهم الإعدادات:

- `LANGUAGE_CODE = 'ar'`: النظام مضبوط للعربية.
- `TIME_ZONE = 'Africa/Cairo'`: توقيت القاهرة.
- `AUTH_USER_MODEL = 'accounts.User'`: يستخدم موديل مستخدم مخصص.
- `LOGIN_URL = 'accounts:login'`: تسجيل الدخول من تطبيق accounts.
- `STATIC_URL`, `STATIC_ROOT`, `MEDIA_URL`, `MEDIA_ROOT`: مضبوطة لتدعم التطوير وبيئة cPanel/Passenger.
- `USE_WHITENOISE`: لتقديم static في الإنتاج عند توفر WhiteNoise.
- إعدادات أمان للكوكيز، CSRF، HSTS، X-Frame-Options.
- إعدادات logging منفصلة للأعمال والأخطاء والأمان.
- دعم Sentry اختياريًا عبر `SENTRY_DSN`.

ملاحظة مهمة: يوجد عدم اتساق بين `README.md` وبعض الإعدادات. README يشير إلى SQLite محليًا، بينما `settings.py` يميل افتراضيًا إلى PostgreSQL. كذلك `.env.example` يحتوي أسماء متغيرات مثل `ENGINE`, `NAME`, `USER`, بينما الكود ينتظر `DB_ENGINE`, `POSTGRES_DB`, `POSTGRES_USER`. هذه نقطة تحتاج تصحيحًا.

## 5. المسارات العامة

ملف `config/urls.py` يجمع المسارات الرئيسية:

- `/`: لوحة التحكم.
- `/admin/`: Django admin.
- `/accounts/`: تسجيل الدخول والمستخدمين.
- `/products/`: المنتجات والكتالوج.
- `/inventory/`: المخازن وحركات المخزون.
- `/customers/`: العملاء وCRM.
- `/orders/`: الطلبات والمبيعات.
- `/invoices/`: الفواتير والطباعة والتصدير.
- `/finance/`: الخزن والحركات المالية.
- `/purchases/`: الموردون والمشتريات.
- `/returns/`: المرتجعات والاستبدالات.
- `/reports/`: التقارير.
- `/sales-reps/`: المناديب والعهد.
- `/settings/`: إعدادات الشركة.
- `/audit/`: سجل التدقيق.
- `/api/`: واجهة المزامنة.
- `/health/`: فحص صحة النظام.
- `/manifest.json`, `/service-worker.js`, `/offline/`: دعم PWA.

## 6. المستخدمون والصلاحيات

تطبيق `accounts` يحتوي موديل المستخدم المخصص `User`. الأدوار الرئيسية:

- `manager`: مسؤول النظام.
- `director`: المدير.
- `sales`: مندوب مبيعات.
- `warehouse`: مسؤول مخزن.

يوجد في `accounts/permissions.py` دوال ومكسنز للتحكم في الوصول:

- المدير والمدير العام والسوبر يوزر لهم صلاحيات واسعة.
- مندوب المبيعات يستطيع الوصول لمسارات البيع والعملاء والتحصيل حسب القيود.
- مسؤول المخزن يستطيع الوصول للمخزون وبعض عمليات المناديب.
- عرض التكلفة والربح مقيد عادة بالمدير.
- الحذف النهائي مقيد بالمدير أو السوبر يوزر.

## 7. المنتجات والكتالوج

تطبيق `products` يدير:

- التصنيفات `Category` مع دعم التصنيف الأب.
- الألوان `Color` مع كود لون اختياري.
- المقاسات `Size` مع ترتيب عرض.
- المنتجات `Product`.
- متغيرات المنتج `ProductVariant`.

المنتج يحتوي على:

- اسم.
- SKU فريد.
- تصنيف.
- مورد اختياري.
- وصف، خامة، موسم.
- سعر قطاعي وسعر جملة.
- عدد القطع في الدستة.
- صورة.
- حالة نشاط.

المتغير يحتوي على:

- المنتج الأب.
- اللون.
- المقاس.
- صورة.
- SKU خاص بالمتغير.
- باركود.
- تكلفة.
- سعر بيع.
- سعر قطاعي.
- سعر جملة.
- حالة نشاط.

يوجد تحقق من عدم كون تكلفة المتغير وسعر البيع أقل من صفر. كما توجد فهارس على الحقول كثيرة الاستخدام مثل SKU والباركود والحالة.

## 8. المخزون

تطبيق `inventory` من أهم أجزاء النظام. يحتوي:

- `Warehouse`: المخازن.
- `Stock`: رصيد صنف معين داخل مخزن معين.
- `StockBatch`: دفعات المخزون وتكلفتها.
- `StockMovement`: سجل كل حركة مخزنية.

أنواع المخازن:

- مخزن رئيسي.
- محل بيع.
- عهدة مندوب.

أنواع حركات المخزون تشمل:

- رصيد افتتاحي.
- دخول مخزون.
- خروج مخزون.
- تحويل بين مخازن.
- بيع.
- مرتجع.
- تسوية.
- استلام شراء.
- مرتجع شراء.
- مرتجع بيع.
- مرتجع تالف.
- خروج استبدال.
- تسليم عهدة لمندوب.
- رجوع عهدة من مندوب.
- بيع مندوب.
- عينة أو إصدار مجاني.

الخدمات في `inventory/services.py` تستخدم `transaction.atomic` و`select_for_update` في العمليات الحساسة. هذا مهم لمنع مشاكل التزامن عند تعديل نفس الرصيد من أكثر من طلب.

تدفقات المخزون الأساسية:

- `stock_in`: يزيد الرصيد، ينشئ batch، ويسجل حركة.
- `stock_out`: ينقص الرصيد، يستهلك من batch، ويسجل حركة.
- `transfer_stock`: ينقص من مخزن ويزيد في آخر مع إنشاء batch في الوجهة.
- `adjust_stock`: يضبط الرصيد إلى رقم جديد.
- `sale_stock`: يخصم المخزون عند البيع.
- `return_stock`: يعيد المخزون عند المرتجع أو الإلغاء.

النظام يستخدم دفعات مخزون، وغالبًا يستهلك الأقدم أولًا حسب `received_at` و`pk`.

## 9. العملاء وCRM

تطبيق `customers` يدير العملاء والتفاعلات معهم.

موديل العميل يحتوي:

- الاسم.
- نوع العميل: قطاعي أو جملة.
- الهاتف.
- واتساب.
- البريد الإلكتروني.
- اسم الشركة.
- الرقم الضريبي.
- العنوان.
- حد الائتمان.
- رصيد افتتاحي.
- ملاحظات.
- المستخدم المنشئ.
- مندوب المبيعات المسؤول.
- حالة النشاط.

موديل `CustomerInteraction` يسجل:

- مكالمة.
- واتساب.
- زيارة.
- ملاحظة.
- شكوى.
- متابعة.

كل تفاعل له عنوان ووصف وتاريخ متابعة وحالة إكمال.

الخدمات في `customers/services.py` توفر:

- تحديد العملاء المرئيين حسب دور المستخدم.
- ملخص العميل.
- إجمالي المشتريات.
- إجمالي المدفوع.
- إجمالي المتبقي.
- آخر طلب.
- آخر تفاعل.
- المتابعة القادمة.
- معدل المرتجعات.
- عدد الشكاوى المفتوحة.
- العملاء الخاملين.
- العملاء المدينين.
- أفضل العملاء.
- لوحة CRM.

## 10. الطلبات والمبيعات

تطبيق `orders` يدير دورة البيع. موديل `Order` يدعم:

- فاتورة بيع.
- عرض سعر.
- عينة أو إصدار مجاني.

أنواع البيع:

- قطاعي B2C.
- جملة B2B.

حالات الطلب:

- مسودة.
- مؤكد.
- قيد التجهيز.
- جاهز.
- مكتمل.
- ملغي.
- مرتجع جزئي.
- مرتجع كامل.

حالات الدفع:

- غير مدفوع.
- مدفوع جزئيًا.
- مدفوع بالكامل.

طرق الدفع:

- نقدي.
- الدفع عند الاستلام.
- تحويل بنكي يدوي.
- تحويل محفظة يدوي.
- آجل.

الطلب يحتوي أيضًا على:

- رقم طلب فريد.
- العميل.
- المخزن.
- الخصومات.
- الإجمالي قبل وبعد الخصم.
- إجمالي التكلفة.
- الربح الإجمالي.
- المدفوع.
- المتبقي.
- المستخدم المنشئ.

بند الطلب `OrderItem` يحتوي:

- المتغير.
- المخزن.
- batch المخزون.
- الكمية.
- السعر الأصلي.
- الخصم.
- السعر النهائي.
- تكلفة الوحدة.
- الإجمالي.
- إجمالي التكلفة.
- إجمالي الربح.

الخدمات في `orders/services.py` مسؤولة عن:

- توليد رقم الطلب.
- اختيار السعر حسب العميل ونوع الطلب.
- حساب حدود الخصم حسب المستخدم.
- منع البيع تحت التكلفة لغير المصرح لهم.
- تحضير تسعير البند.
- حساب إجماليات الطلب.
- إنشاء الطلب.
- حفظ المسودة.
- تأكيد الطلب وخصم المخزون.
- إلغاء الطلب وإرجاع المخزون.
- عمل مرتجع كامل للطلب.

نقطة فنية مهمة: توليد رقم الطلب يعتمد على عدد طلبات اليوم + 1، وقد يتصادم في حالات ضغط متزامن. الأفضل مستقبلًا استخدام sequence أو retry عند التصادم.

## 11. الفواتير والطباعة

تطبيق `invoices` يحتوي موديل `Invoice` المرتبط One-to-One بالطلب.

الفاتورة تحتوي:

- الطلب.
- رقم الفاتورة.
- وقت الإصدار.
- عدد مرات الطباعة.

الخدمات تمنع إصدار فاتورة من طلب مسودة أو عرض سعر غير مؤكد. وإذا كانت الفاتورة بيعًا غير آجل، يتم تسجيل تحصيل تلقائي حسب المنطق المالي.

يدعم النظام:

- عرض الفاتورة.
- طباعة الفاتورة.
- تعليم الفاتورة كمطبوعة.
- إضافة دفعة من صفحة الفاتورة.
- تصدير الفواتير Excel.
- تصدير الفواتير PDF.
- طباعة تقرير فواتير.

الطباعة الحرارية مدعومة من إعدادات الشركة:

- عرض ورق 80mm أو 58mm.
- حجم خط الفاتورة.
- طباعة من المتصفح.
- طباعة مباشرة عبر Electron.
- طباعة مباشرة عبر QZ Tray.
- اسم الطابعة الحرارية.

## 12. المالية والخزن

تطبيق `finance` يدير الخزن والحركات المالية.

موديل `CashAccount` يدعم أنواع:

- خزنة نقدية.
- حساب بنكي.
- محفظة إلكترونية.
- عهدة مالية لمندوب.

كل خزنة لها:

- اسم.
- نوع.
- مستخدم مرتبط اختياريًا.
- رصيد.
- السماح بالسحب على المكشوف.
- حالة نشاط.

موديل `PaymentTransaction` يسجل كل حركة مالية:

- تحصيل من عميل.
- دفع لمورد.
- مصروف.
- رد للعميل.
- تحصيل مندوب.
- تسليم مندوب.
- تحويل بين الخزن.
- تسوية.
- خصم مسموح لعميل.

كل حركة لها:

- نوع.
- اتجاه داخل أو خارج.
- مبلغ.
- خزنة.
- طلب مرتبط.
- عميل مرتبط.
- مندوب مرتبط.
- مورد مرتبط.
- مرجع.
- ملاحظات.
- تاريخ الحركة.
- هل تؤثر على رصيد الخزنة.
- المستخدم المنشئ.

الخدمات المالية تقوم بـ:

- قفل الخزنة قبل تعديل الرصيد.
- منع الصرف بدون رصيد إن لم يكن overdraft مفعّلًا.
- تسجيل تحصيل عميل.
- تحصيل رصيد العميل وتوزيعه على الرصيد الافتتاحي ثم الفواتير المفتوحة.
- رد أموال للعميل.
- تسجيل خصم مسموح لا يؤثر على الخزنة.
- بناء كشف حساب العميل.
- بناء كشف حساب الخزنة.
- دفع مورد.
- تسجيل تحصيل تلقائي لفاتورة بيع.
- رد تلقائي عند إلغاء طلب.
- تحصيل دفعة على طلب.
- إضافة مصروف.
- حذف حركة وعكس أثرها.
- تحويل بين الخزن.
- تسجيل تحصيل مندوب.

هذه الطبقة مركزية جدًا، وأي تعديل فيها يؤثر على التقارير والأرصدة والفواتير.

## 13. المشتريات والموردون

تطبيق `purchases` يدير الموردين وأوامر الشراء.

موديل `Supplier` يحتوي:

- الاسم.
- الهاتف.
- البريد.
- العنوان.
- اسم الشركة.
- رصيد افتتاحي.
- الرصيد الحالي.
- ملاحظات.
- حالة نشاط.

موديل `PurchaseOrder` يحتوي:

- رقم أمر شراء.
- المورد.
- الحالة.
- تاريخ الطلب.
- تاريخ متوقع.
- الإجمالي.
- المدفوع.
- المتبقي.
- ملاحظات.
- المستخدم المنشئ.

حالات أمر الشراء:

- مسودة.
- تم الطلب.
- مستلم جزئيًا.
- مستلم بالكامل.
- ملغي.

موديل `PurchaseOrderItem` يحتوي:

- أمر الشراء.
- متغير المنتج.
- الكمية.
- الكمية المستلمة.
- تكلفة الوحدة.
- إجمالي التكلفة.

الخدمات تقوم بـ:

- توليد رقم أمر الشراء.
- إعادة حساب الإجمالي.
- إنشاء أمر شراء.
- زيادة رصيد المورد عند إنشاء أمر غير مسودة.
- استلام أصناف الشراء إلى المخزون.
- تحديث تكلفة المنتج عند الاستلام.
- دفع المورد.
- إلغاء أمر الشراء بشروط.
- إنشاء مرتجع شراء وخصم المخزون وتقليل رصيد المورد.

## 14. المرتجعات والاستبدال

تطبيق `returns` يدير مرتجعات البيع والاستبدال.

موديل `SalesReturn` يحتوي:

- الطلب.
- العميل.
- نوع المرتجع.
- الحالة.
- السبب.
- مبلغ الاسترداد.
- منشئ المرتجع.
- المعتمد.
- المنفذ.

أنواع المرتجع:

- استرداد.
- استبدال.
- مرتجع جزئي.

الحالات:

- مسودة.
- معتمد.
- مكتمل.
- مرفوض.

موديل `SalesReturnItem` يحتوي:

- المرتجع.
- بند الطلب الأصلي.
- المنتج.
- الكمية.
- حالة المنتج: سليم، تالف، يحتاج مراجعة.
- هل يعود للمخزون.
- مبلغ الاسترداد.

موديل `ExchangeItem` يحتوي:

- المرتجع.
- البند القديم.
- المنتج الجديد.
- الكمية.
- السعر الجديد.
- السعر القديم.
- فرق السعر.

الخدمات تقوم بـ:

- إنشاء مرتجع.
- إضافة بند مرتجع.
- حساب الكمية المتاحة للإرجاع.
- إضافة بند استبدال.
- اعتماد المرتجع.
- إكمال المرتجع.
- إعادة المخزون للصالح.
- تسجيل المرتجع التالف كحركة.
- خصم المنتج الجديد في الاستبدال.
- تسجيل فرق السعر كتحصيل أو رد.
- تقليل مديونية الطلب أو رد النقدية.
- تحديث حالة الطلب إلى مرتجع جزئي أو كامل.

## 15. المناديب والعهد

تطبيق `sales_reps` يدير عهدة مناديب المبيعات.

موديل `SalesRepStockAssignment` يتابع:

- المندوب.
- المنتج.
- المخزن المصدر.
- الكمية المسلمة.
- الكمية المباعة.
- الكمية المرتجعة.
- الكمية المتبقية.
- من سلّم العهدة.
- التاريخ.
- الملاحظات.
- هل العهدة نشطة.

موديل `SalesRepCollection` يتابع:

- المندوب.
- العميل.
- الطلب.
- المبلغ المحصل.
- المبلغ المسلم للإدارة.
- الخزنة.
- تاريخ التحصيل.
- هل تم التسليم.
- وقت التسليم.
- الملاحظات.

الخدمات تقوم بـ:

- التأكد أن المستخدم مندوب مبيعات.
- إنشاء خزنة خاصة بالمندوب.
- تسليم مخزون لمندوب.
- إرجاع مخزون من المندوب.
- تسجيل بيع من عهدة المندوب.
- تسجيل تحصيل مندوب.
- تسليم نقدية المندوب إلى خزنة الإدارة.
- بناء كشف عهدة وتحويلات المندوب.

ملاحظة فنية: بعض عمليات المناديب تعدل المخزون أو العهدة دون استخدام نفس منطق `StockBatch` الموجود في خدمات المخزون الأساسية، وهذا يحتاج مراجعة إذا كان تتبع تكلفة الدفعات مهمًا بدقة.

## 16. التقارير

تطبيق `reports` يقدم تقارير كثيرة، منها:

- تقرير المبيعات.
- تقرير الربحية.
- تقرير صافي الربح.
- تقرير ديون العملاء.
- تقرير العملاء الخاملين.
- تقرير الخصومات.
- تقرير عهدة مندوبي المبيعات.
- تقرير تحصيلات المناديب.
- تقرير المخزون المنخفض.
- تقرير المنتجات الراكدة.
- تقرير المرتجعات.
- تقرير المشتريات.
- تقرير مستحقات الموردين.
- تقرير المبيعات اليومية.
- تقرير المبيعات الشهرية.
- تقرير المبيعات السنوية.
- تقرير المخزون.
- تقرير العملاء.
- تقرير الموظفين.
- تقرير حركة المخزون.

يوجد دعم تصدير عام في `config/exports.py` لتصدير CSV وPDF.

## 17. لوحة التحكم

تطبيق `dashboard` يعرض واجهات مختلفة حسب دور المستخدم:

- المدير يرى مؤشرات إدارية ومالية ومخزنية.
- مندوب المبيعات يرى واجهة مناسبة للمبيعات والتحصيل.
- مسؤول المخزن يرى واجهة مخزون.

تعتمد لوحة التحكم على تقارير وخدمات النظام لعرض مؤشرات مثل المبيعات، المخزون المنخفض، والتحركات المهمة.

## 18. إعدادات الشركة

تطبيق `settings_app` يحتوي موديل `CompanySettings`. الإعدادات تشمل:

- اسم الشركة.
- الهاتف.
- البريد.
- العنوان.
- الرقم الضريبي.
- الشعار.
- ملاحظات الفاتورة.
- عرض ورق الفاتورة الحرارية.
- حجم خط الفاتورة الحرارية.
- طريقة الطباعة الحرارية.
- اسم الطابعة.
- الحد الأقصى لخصم مندوب المبيعات.
- السماح للمدير بالبيع تحت التكلفة.

يتم تحميل الإعدادات عبر `CompanySettings.load()`، وهي تعتمد على سجل واحد غالبًا `pk=1`.

## 19. PWA والعمل دون اتصال

النظام يدعم PWA عبر:

- `static/manifest.json`.
- `static/service-worker.js`.
- `templates/offline.html`.
- ملفات JavaScript داخل `static/js/pwa/`.
- API داخل `sync_api`.

الـ service worker يقوم بـ:

- تخزين ملفات وواجهات أساسية.
- تقديم صفحة offline عند انقطاع الاتصال.
- اعتراض عمليات POST عند فشل الشبكة.
- تحويل العملية إلى payload.
- تخزينها في IndexedDB.
- وضعها في sync queue.
- إرسال رسالة للصفحات عند عودة الاتصال لمعالجة المزامنة.

IndexedDB يستخدم قاعدة باسم `sh-offline-db`، وبداخلها stores مثل:

- `sales`.
- `customers`.
- `products`.
- `product_variants`.
- `stock`.
- `warehouses`.
- `stock_movements`.
- `cash_accounts`.
- `cash_transactions`.
- `driver_actions`.
- `sync_queue`.
- `metadata`.

واجهة `sync_api` توفر:

- تسجيل دخول token.
- refresh token.
- بيانات المستخدم.
- bootstrap للبيانات المسموحة.
- changes منذ وقت معين.
- push للعمليات المعلقة.
- endpoints خاصة بالمتصفح تعتمد على session auth.

المزامنة تدعم idempotency key لمنع تكرار نفس العملية. كما يوجد منطق للتعامل مع التعارضات بناءً على timestamps.

ملاحظة صيانة مهمة: ملف `sync_api/services.py` يحتوي تعريفات مكررة لبعض الدوال مثل `process_customer` و`process_order`. في Python، التعريف الأخير يطغى على السابق، لذلك التعريفات الأولى تصبح كودًا ميتًا. هذا لا يعني بالضرورة أن النظام لا يعمل، لكنه يجعل الملف صعب الصيانة ويحتاج تنظيفًا.

## 20. سجل التدقيق Audit

تطبيق `audit` يحتوي موديل `AuditLog` لتسجيل العمليات الحساسة.

السجل يحتوي:

- المستخدم.
- الفعل.
- القسم.
- اسم الموديل.
- معرف العنصر.
- تمثيل العنصر نصيًا.
- القيم قبل التغيير.
- القيم بعد التغيير.
- IP.
- user agent.
- ملاحظات.
- وقت الإنشاء.

الأفعال تشمل:

- إنشاء.
- تعديل.
- حذف.
- تأكيد.
- إلغاء.
- مرتجع.
- استلام.
- دفع.
- تحصيل.
- تسليم عهدة.
- تعيين.
- تسوية.
- تحويل.

الأقسام تشمل:

- المنتجات.
- الطلبات.
- المخزون.
- المشتريات.
- المرتجعات.
- المالية.
- العملاء.
- المناديب.
- الإعدادات.
- الحسابات.
- الفواتير.

الموديل يمنع تعديل أو حذف سجلات التدقيق على مستوى `save` و`delete`.

## 21. الصحة والمراقبة والنسخ الاحتياطي

يوجد endpoint `/health/` في `config/urls.py` يفحص:

- الاتصال بقاعدة البيانات.
- إمكانية الكتابة داخل `MEDIA_ROOT`.
- مساحة القرص.
- وجود migrations غير مطبقة.

في وضع DEBUG يعرض تفاصيل، وفي الإنتاج يعرض حالة عامة فقط.

يوجد ملف `BACKUP.md` يشرح النسخ الاحتياطي. أوامر النسخ الاحتياطي:

- `backup_db`: إنشاء backup.
- `backup_upload`: رفع backup إلى S3-compatible storage.

يدعم النسخ الاحتياطي:

- SQLite.
- PostgreSQL.
- تشفير اختياري عبر Fernet.
- تحقق من سلامة الملف.
- اختبار restore لـ SQLite.
- رفع خارج السيرفر اختياري.

ملاحظة: `cryptography` و`boto3` ليستا ضمن `requirements.txt`، لذلك ميزات التشفير والرفع تحتاج تثبيتًا منفصلًا إن استُخدمت.

## 22. الواجهات والقوالب

القوالب منظمة داخل `templates/` حسب كل تطبيق. يوجد قالب عام `base.html` وpartials مشتركة:

- navbar.
- sidebar.
- pagination.
- messages.
- export buttons.
- confirm delete.
- workflow actions.
- icon sprite.

يوجد CSS رئيسي في `static/css/main.css`، وملفات JS كثيرة منها:

- `orders.js`.
- `inventory.js`.
- `product_create.js`.
- `purchase_create.js`.
- `purchase_return.js`.
- ملفات PWA للمزامنة والعمل offline.

## 23. الاختبارات

يوجد ملفات اختبارات في معظم التطبيقات:

- `accounts/tests.py`.
- `audit/tests.py`.
- `audit/tests_security.py`.
- `config/tests.py`.
- `customers/tests.py`.
- `dashboard/tests.py`.
- `finance/tests.py`.
- `inventory/tests.py`.
- `inventory/tests_concurrency.py`.
- `invoices/tests.py`.
- `orders/tests.py`.
- `orders/tests_constraints.py`.
- `products/tests.py`.
- `purchases/tests.py`.
- `reports/tests.py`.
- `returns/tests.py`.
- `sales_reps/tests.py`.
- `settings_app/tests.py`.

بسبب عدم وجود قاعدة البيانات الأصلية في هذا المسار، لا ينبغي الاعتماد على نتائج تشغيل الاختبارات هنا لتقييم بيانات الإنتاج. التحليل الحالي مبني على الكود والملفات فقط.

## 24. ملفات build وdesktop

يوجد في المسار مجلدات:

- `desktop-dist`.
- `backend-dist`.
- `dist`.
- `build`.
- `node_modules`.

هذه تبدو كمخرجات build أو بيئة Desktop/Electron، وليست جزءًا أساسيًا من كود Django المتتبع. أغلبها مستبعد في `.gitignore`.

يوجد installer باسم `SH Desktop Setup 1.0.0.exe` داخل مجلدات التوزيع، ما يدل أن النظام قد تم تجهيزه سابقًا كنسخة سطح مكتب أو تطبيق مرتبط بواجهة Electron.

## 25. نقاط القوة

أبرز نقاط القوة في النظام:

- تقسيم جيد حسب المجالات.
- وجود طبقة services للمنطق الحساس.
- استخدام transactions وlocks في المال والمخزون.
- دعم أدوار وصلاحيات.
- دعم تقارير كثيرة.
- دعم Audit Trail.
- دعم PWA والعمل دون اتصال.
- دعم طباعة حرارية.
- دعم PDF بالعربية.
- دعم الموردين والمشتريات وليس المبيعات فقط.
- دعم عهدة المناديب والتحصيلات.
- وجود اختبارات موزعة على معظم التطبيقات.

## 26. المخاطر والملاحظات المهمة

أهم الملاحظات التي يجب التعامل معها:

1. توجد بيانات اعتماد قاعدة بيانات حقيقية الشكل داخل `settings.py` و`.env.example`. يجب حذفها من الكود وتدوير كلمة المرور.
2. لا يوجد `.env` في المسار الحالي، لذلك التشغيل يعتمد على defaults غير مناسبة غالبًا.
3. عدم اتساق أسماء متغيرات الداتابيز بين `.env.example` و`settings.py`.
4. README يقول SQLite محليًا، بينما الكود يميل إلى PostgreSQL افتراضيًا.
5. توليد أرقام الطلبات والفواتير بالاعتماد على count اليومي قد يتصادم عند التزامن.
6. بعض عمليات المرتجعات والمناديب لا تتعامل مع `StockBatch` بنفس دقة خدمات المخزون الأساسية.
7. `sync_api/services.py` يحتوي كودًا مكررًا ودوالًا مطموسة بتعريفات لاحقة.
8. CORS مفتوح على `/api/` بالكامل لدعم Electron، وهذا يحتاج تقييدًا في الإنتاج إن أمكن.
9. ميزات النسخ الاحتياطي المشفرة والرفع الخارجي تحتاج حزمًا غير مذكورة في `requirements.txt`.
10. بعض النصوص العربية تظهر في مخرجات الطرفية بترميز مكسور، ويجب التأكد من أن الملفات محفوظة UTF-8 وأن بيئة الطرفية تدعم UTF-8.

## 27. توصيات تحسين قبل الإنتاج

الأولوية العالية:

- حذف أي أسرار من `settings.py` و`.env.example`.
- تدوير بيانات اعتماد قاعدة الإنتاج إن كانت مستخدمة فعلًا.
- توحيد إعدادات الداتابيز في README و`.env.example` و`settings.py`.
- إنشاء `.env` إنتاجي آمن خارج Git.
- ضبط `SESSION_COOKIE_SECURE=True` و`CSRF_COOKIE_SECURE=True` وHTTPS/HSTS في الإنتاج.
- مراجعة CORS في `SyncApiCorsMiddleware`.

الأولوية المتوسطة:

- تحسين توليد أرقام الطلبات والفواتير والمشتريات.
- تنظيف `sync_api/services.py`.
- توحيد أثر حركات المخزون على `StockBatch`.
- إضافة tests موجهة لتزامن أرقام الطلبات والفواتير.
- إضافة `cryptography` و`boto3` إلى extras أو توثيق تثبيتهما بوضوح.

الأولوية الأقل:

- تحسين توثيق flows للمستخدم النهائي.
- إضافة مخطط ERD.
- إضافة API docs أكثر تفصيلًا.
- تحسين مخرجات PDF والطباعة حسب طابعات العملاء.
- إضافة تنبيهات بريدية أو داخلية للمخزون المنخفض والأخطاء الحرجة.

## 28. خلاصة

النظام الحالي هو ERP متقدم لإدارة نشاط ملابس، ويحتوي على أغلب الوحدات المطلوبة لتشغيل البيع والمخزون والمالية والمشتريات والمرتجعات والمناديب. البنية العامة جيدة، ووجود services وtransactions وAudit وPWA يعطيه أساسًا قويًا.

مع ذلك، قبل الاعتماد الإنتاجي الكامل، يجب التركيز على الأمان وإعدادات البيئة وتنظيف بعض الديون التقنية، خصوصًا أسرار الداتابيز، إعدادات `.env`، منطق المزامنة الكبير، واتساق دفعات المخزون مع كل الحركات.

